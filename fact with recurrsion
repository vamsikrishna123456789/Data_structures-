#include<stdio.h>
factorial(int n)
{
	int n;
	printf("enter n value:");
	scanf("%d",&n);
	printf("the %d is the factorial of %d",factorial(n),n);
	return 0;
}
int factorial(int n)
{
	int i,fact=1;
	for(i=1;i<=n;i++)
	fact=fact*i;
	return(fact);
}
